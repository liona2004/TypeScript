typescript следит за типами данных, нельзя присвоить значение другого типа данных. Но если пустой объект, то можно присвоить

компилятор typescript подказывает на ошибки написания названий переменных 

typescript анализирует ваши данные и следит за ошибками

Если просто объявить переменную, ей можно будет присвоить значение любого типа данных
let b;
let b: any; // но лучше указывать точный тип
let b: string;
b = "lala";

let b = "lala";

let b: Array<number>;
b = [1,2,3,4,5];

let b: Array<number | string>;
b = [1,2,3,4,5, "hello", "yo"];
